This is a backend task where I created a logging middleware and a notification service.

### Logging Middleware

The logging middleware is used to log the messages to the evaluation server.

### Notification Service

The notification service is used to get the top 'n' notifications from the evaluation server.